/**
 * Script that gets executed after page load to determine if page needs to be
 * logged or not. If the page gives a 404 or similar error if it does then
 * ignore if its okay then we just call log and log the site visit for our
 * statistics.
 */
